name: Weekly update action
on: [push]

jobs:
  weekly-update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0
        with:
            ref: 'main'
            submodules: 'recursive'
        
      - run: |
          uname -a
